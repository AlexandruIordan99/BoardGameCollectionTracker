/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface UploadBoardGameSplashArt$Params {
  'boardgame-id': number;
      body?: {
'file': Blob;
}
}

export function uploadBoardGameSplashArt(http: HttpClient, rootUrl: string, params: UploadBoardGameSplashArt$Params, context?: HttpContext): Observable<StrictHttpResponse<{
}>> {
  const rb = new RequestBuilder(rootUrl, uploadBoardGameSplashArt.PATH, 'patch');
  if (params) {
    rb.path('boardgame-id', params['boardgame-id'], {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      }>;
    })
  );
}

uploadBoardGameSplashArt.PATH = '/boardgame/cover/{boardgame-id}';
